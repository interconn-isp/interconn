%nav.navbar.navbar-default.navbar-fixed-top
  .container
    .navbar-header
      %button.navbar-toggle{ type: 'button',
      'data-toggle' => 'collapse',
      'data-target' => '.navbar-collapse' }
        %span.sr-only Mostra/nascondi navigazione
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      = link_to 'InterConn', root_path, class: 'navbar-brand'
    .collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li{ class: ('active' if params[:id] == 'home') }
          = link_to 'Home', root_path
        %li{ class: ('active' if params[:id].to_s.start_with?('pricing/')) }
          = link_to 'Piani', pricing_wadsl_path
        %li{ class: ('active' if params[:id] == 'support' || params[:controller] == 'tickets') }
          = link_to 'Supporto', support_path
